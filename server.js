import { createServer } from 'node:http';import { readFile } from 'node:fs/promises';import { extname } from 'node:path';const types={'.html':'text/html','.js':'text/javascript','.css':'text/css'};createServer(async(req,res)=>{try{let p=req.url==='/'?'/index.html':req.url;let data=await readFile('.'+p);res.writeHead(200,{'Content-Type':types[extname(p)]||'text/plain'});res.end(data)}catch{res.writeHead(404);res.end('Not found')}}).listen(4173,()=>console.log('DukeDrop: http://localhost:4173'));
